#include <iostream>
#include <string>
#include <vector>

std::vector<int> SVO_function(const std::string& s) {
  std::vector<int> SVO(s.size(), 0);
  int l = 0;
  int r = 0;

  for (int i = 1; i < s.size(); ++i) {
    b
  }
}

int main() {
  SVO_function("abacaba");

  return 0;
}